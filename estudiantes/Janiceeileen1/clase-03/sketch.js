//intento de cambiar de color según el click
function setup() {
  createCanvas(800, 800);
}

function draw() {
  if(traingle) {
    concole.log("triangle red" );
  }else if("clickEnBackground");
    console.log("triangle green");
  }
  background(80, 35, 66);
  triangle(mouseX, mouseY, 700, 200, 600, 600)
  
  fill(200, 0, 50);
  noStroke();
  triangle(width/5, height/ 80, 90, 75, 50);
}